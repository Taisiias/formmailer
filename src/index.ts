// TODO: finish AJAX test form
// TODO: return { result: "ok" } or { result: "error", description: "error message" }
// instead of redirect on AJAX request.
// TODO: content-type = application/json in AJAX response.

import { runAndReport } from "./run";

runAndReport();
